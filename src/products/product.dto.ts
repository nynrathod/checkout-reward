export interface ProductRow {
  id: string;
  name: string;
  price_cents: number;
  inventory: number;
}

export interface ProductDto {
  id: string;
  name: string;
  price_cents: number;
  inventory: number;
}
